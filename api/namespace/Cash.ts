import {Nothing} from "../types/Nothing";
import {Rule} from "../classes/impl/enums/Rule";

export namespace Cash {

    export const USER_TAGS : Map<string, number> = new Map()

    export const ALIASES_DICTIONARY : Map<string, Rule> = new Map([

    ])

    export async function synchronize() : Nothing {

    }

}